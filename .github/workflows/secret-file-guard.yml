name: Secret File Guard

on:
  pull_request:
  push:

jobs:
  deny-secret-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Block secret file patterns
        run: ./.ci/check-no-secret-files.sh

